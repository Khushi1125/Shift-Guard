# Shift-Guard — Python dependencies
# Install with:  pip install -r requirements.txt
# (Jupyter is also needed to run the notebooks: pip install jupyter)

numpy==2.4.6
pandas==3.0.3
scipy==1.17.1
scikit-learn==1.9.0
matplotlib==3.11.0
seaborn==0.13.2
joblib==1.5.3

# ONNX export (baseline.ipynb → outputs/baseline_model.onnx)
onnx==1.22.0
skl2onnx==1.20.0
onnxruntime==1.27.0

# Semantic analysis (src/semantic_analysis.py)
deepgram-sdk==7.3.1
vaderSentiment==3.3.2
python-dotenv==1.2.2

# Tone analysis (src/tone_analysis.py)
torch
torchaudio
transformers
huggingface_hub
soundfile          # WAV I/O (no native codec required)
scipy              # polyphase resampling for non-16kHz audio

# Testing
pytest>=9.0
