# Shift-Guard — Python dependencies
# Install with:  pip install -r requirements.txt

# Core data / ML
numpy==2.4.6
pandas>=2.0.0,<3.0.0
scipy==1.17.1
scikit-learn==1.9.0
matplotlib==3.11.0
seaborn==0.13.2
joblib==1.5.3

# ONNX export (model/notebooks/baseline.ipynb → model/outputs/baseline_model.onnx)
onnx==1.22.0
skl2onnx==1.20.0
onnxruntime==1.27.0

# Semantic analysis (model/src/semantic_analysis.py)
deepgram-sdk==7.3.1
vaderSentiment==3.3.2
python-dotenv==1.2.2

# Tone analysis (model/src/tone_analysis.py)
torch
torchaudio
transformers
huggingface_hub
soundfile
scipy

# API / backend
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.0

# Arduino bridge
pyserial==3.5
requests==2.31.0

# Arize monitoring
arize==8.2.0

# Voice transcription (mic upload -> WAV conversion -> Deepgram STT)
# deepgram-sdk is already pinned above (semantic analysis); these are the extra bits:
pydub==0.25.1               # WebM/Opus -> WAV (shells out to ffmpeg)
audioop-lts                   # Python 3.13 audioop compatibility fix
python-multipart==0.0.32   # FastAPI file uploads

# Testing
pytest>=9.0